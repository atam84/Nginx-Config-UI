# Fail2Ban filter for Nginx Config UI
# Place in /etc/fail2ban/filter.d/nginx-ui.conf
# Requires app to log 401 responses with client IP (or use nginx access log in front)

[Definition]
failregex = ^<HOST> -.*"POST /api/auth/login HTTP.*" 401
ignoreregex =
